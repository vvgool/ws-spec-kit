# 领域文档

## 布局

这是一个单上下文仓库。领域词汇在根目录 `CONTEXT.md` 存在时维护，架构决策存放在 `docs/adr/`。

## 使用规则

- 探索领域行为前，如存在根目录 `CONTEXT.md`，应先阅读它。
- 修改架构决策前，应先阅读 `docs/adr/` 中相关记录。
- 在规格、任务、测试和实现中一致使用词汇表定义的术语。
- 若变更与 ADR 冲突，应明确提出冲突，不能静默覆盖。
