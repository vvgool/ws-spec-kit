# 测试资产变化后的真实恢复验收

日期：2026-09-21。代码状态：本地工作区修复，尚未提交或发布。

## 问题与修复

2048.IO 的 Work Item `WSS-01M31CQSS9DN1Z4VJ24VDPFC7P` 在实现期间调整测试辅助资产后无法恢复。事件日志的 17 条记录序号与哈希链有效；旧 Red 绑定的专项测试未变，测试资产摘要已变化。原恢复代码把证据过期归为事件断链，inspect 的公开错误目录又将其掩盖为内部错误。

inspect/status 现在继续校验事件及证据结构，但允许读取过期状态并提供恢复建议。原专项测试保持不变时，recover 在隔离的原产品基线上使用当前测试资产重验；只有原失败断言仍然失败才记录新 Red。产品修改仍相对原产品基线检查，不因恢复而丢失 modifiedFiles 约束。

本次还修复了任务工作树将 node_modules 链接到同仓库已登记 checkout 时无法复制依赖的问题。恢复复制真实依赖字节并重定位链接，拒绝其他外部依赖根；不会共享可变依赖文件。普通 .DS_Store 不参与测试证据，软链接校验保留。

## 真实项目结果

- 使用修复后的 CLI 执行一次成功的 recover，隔离原产品基线产生 8 个原专项断言失败，退出码 1；引擎生成新的 Red，旧 Red 与恢复原因保留在事件中。
- Work Item 从不可恢复变为 implement ready，inspect 返回 `nextAction.kind: acquire`。
- 恢复前后，游戏主目录和实际任务工作树的 workspace digest 均相同；产品、测试和文档改动未被恢复操作覆盖。
- 当前全局安装的 beta.16 CLI 已能 inspect 此已恢复任务。全局安装未替换，未来同类漂移仍需包含本修复的版本。

## 验证边界

回归覆盖正常证据重验、辅助资产新增、产品修改申报、原专项测试被修改时拒绝、全绿不能生成 Red、活动租约及后续证据拒绝、真实事件断链、同仓库依赖隔离与外部依赖拒绝。

类型检查、构建通过。全量首轮 1110/1111，通过之外唯一失败为公开错误码文档表未同步；修正后 Contract 85/85。随后对最终恢复提示逻辑及高层命令执行针对性回归，23/23 通过。全量未在文档修正后重复执行，不能将首轮记录改写成全绿。

发布 review 又发现链接依赖下 Vitest 的 runner 身份只按工作区映射，无法匹配依赖真实来源。已修正 runner 路径和依赖图映射，并以实际 Vitest Gate 复现先失败后通过；增加隔离 runner 改写时拒绝、原依赖身份不变的断言。ADR 0005 已同步此次恢复边界修订。最终发布另外要求版本提交的 CI 全部必需作业成功，不使用上述历史失败记录替代发布门禁。

本次验收只确认工作流恢复。未代替游戏任务执行 implement 提交、Green Gate、review、Git 提交、Issue 或 Wiki 更新，也不将历史游戏测试结果转录成新的 Gate 证据。
