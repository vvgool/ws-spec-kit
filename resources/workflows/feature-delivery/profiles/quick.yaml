version: 1
profile:
  id: quick
  workflow: feature-delivery
steps:
  clarify: { approval: false, artifactLevel: compact }
  design: { enabled: false }
  plan: { artifactLevel: compact }
  review-fix: { maxIterations: 1 }
  verify-green: { gates: [test] }
publishing: { issueRequired: false, knowledgeRequired: false }
audit: { level: standard }
