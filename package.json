{
  "name": "ws-spec-kit",
  "version": "0.1.0-beta.2",
  "description": "由 Agent Skill 驱动的可定制软件交付工作流引擎",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "wspec": "./dist/cli/main.js"
  },
  "files": [
    "dist",
    "resources",
    "schemas",
    "LICENSE"
  ],
  "exports": {
    "./resources/catalog": "./dist/resources/catalog.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "test:contract": "node --import tsx --test tests/contract/**/*.test.ts",
    "test:e2e": "node --import tsx --test tests/e2e/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "schemas:generate": "tsx src/schemas/generate.ts schemas"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "canonicalize": "^2.1.0",
    "ulid": "^3.0.2",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
